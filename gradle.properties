org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true
kotlin.code.style=official

# Publishing coordinates
GROUP=io.github.rezaiyan
VERSION_NAME=2.0.0
POM_ARTIFACT_ID=levelprogressbar

# Sonatype credentials (set in ~/.gradle/gradle.properties or CI secrets)
# SONATYPE_USERNAME=
# SONATYPE_PASSWORD=

# GPG signing (set in ~/.gradle/gradle.properties or CI secrets)
# SIGNING_KEY_ID=
# SIGNING_KEY=
# SIGNING_PASSWORD=
